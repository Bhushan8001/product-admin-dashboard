"use client";
import Link from "next/link";
import { Product } from "@/lib/types";
import { formatCategory, formatPrice } from "@/lib/utils";
export default function ProductCard({ product, onDelete }: { product: Product; onDelete: (product: Product) => void }) { return <div className="rounded-xl border bg-white p-4 shadow-sm md:hidden"><div className="flex gap-4"><img src={product.thumbnail} alt={product.title} className="h-24 w-24 rounded-lg object-cover" /><div className="min-w-0 flex-1"><Link href={`/products/${product.id}`} className="font-semibold hover:text-blue-600">{product.title}</Link><p className="mt-1 text-xs text-gray-500">{formatCategory(product.category)}</p><div className="mt-2 flex flex-wrap gap-2 text-sm"><span className="font-semibold">{formatPrice(product.price)}</span><span>Rating: {product.rating.toFixed(1)}</span><span>Stock: {product.stock}</span></div></div></div><div className="mt-4 flex gap-2"><Link href={`/products/${product.id}/edit`} className="flex-1 rounded-lg border px-3 py-2 text-center text-sm">Edit</Link><button type="button" onClick={() => onDelete(product)} className="flex-1 rounded-lg bg-red-50 px-3 py-2 text-sm text-red-600">Delete</button></div></div>; }

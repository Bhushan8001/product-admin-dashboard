"use client";
import Link from "next/link";
import { Product } from "@/lib/types";
import { formatCategory, formatPrice } from "@/lib/utils";
export default function ProductTable({ products, onDelete }: { products: Product[]; onDelete: (product: Product) => void }) { return <div className="hidden overflow-hidden rounded-xl border bg-white shadow-sm md:block"><div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b bg-gray-50 text-xs uppercase text-gray-500"><tr>{["Product", "Category", "Price", "Rating", "Stock", "Actions"].map(label => <th key={label} className="px-5 py-4">{label}</th>)}</tr></thead><tbody className="divide-y">{products.map(product => <tr key={product.id} className="hover:bg-gray-50"><td className="px-5 py-4"><div className="flex items-center gap-3"><img src={product.thumbnail} alt={product.title} className="h-12 w-12 rounded-lg object-cover" /><div><Link href={`/products/${product.id}`} className="font-medium hover:text-blue-600">{product.title}</Link><p className="text-xs text-gray-500">#{product.id}</p></div></div></td><td className="px-5 py-4">{formatCategory(product.category)}</td><td className="px-5 py-4 font-medium">{formatPrice(product.price)}</td><td className="px-5 py-4">{product.rating.toFixed(1)}</td><td className="px-5 py-4">{product.stock}</td><td className="px-5 py-4"><div className="flex justify-end gap-2"><Link href={`/products/${product.id}/edit`} className="rounded-md border px-3 py-1.5 text-xs">Edit</Link><button type="button" onClick={() => onDelete(product)} className="rounded-md bg-red-50 px-3 py-1.5 text-xs text-red-600">Delete</button></div></td></tr>)}</tbody></table></div></div>; }
